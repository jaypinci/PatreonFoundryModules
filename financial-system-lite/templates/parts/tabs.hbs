<nav class="sheet-tabs tabs" data-group="primary">
  {{#each tabs as |tab id|}}
    <a class="item {{tab.cssClass}}" data-action="changeTab" data-tab="{{tab.id}}" data-group="{{tab.group}}">
      <i class="{{tab.icon}}"></i> {{localize tab.label}}
    </a>
  {{/each}}
</nav>
